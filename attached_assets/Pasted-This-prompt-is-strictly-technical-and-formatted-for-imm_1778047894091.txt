This prompt is strictly technical and formatted for immediate execution by your System Architect agent. It addresses the UI layout, the math for the radar chart, and the functional batch-processing requirements.
# **TECHNICAL SPECIFICATION: SYSTEM PATCH 4.1 (UI & ENGINE REFACTOR)**
**Objective:** Finalize graphical precision, implement multi-type previews, and build the batch-processing interface.
### **I. RADAR (SPIDER) CHART CALIBRATION**
 * **Coordinate Mapping:** Fix the vertex scaling. Values V_i must map linearly to radius R_i from the origin (0) to the max radius (R_{max}). Currently, all points snap to R_{max}; this must be corrected to reflect actual data.
 * **Concentric Web Grid:** Render concentric polygon "web" lines at 25\%, 50\%, and 75\% intervals of R_{max}.
 * **Guide Typography:** Add light-grey, small-font value markers on the vertical axis of the web grid for user reference.
 * **Polygon Styling:** Apply a **Light Silver Tint** (rgba(192, 192, 192, 0.15)) to the area bounded by the radar polygon.
 * **Icon Removal:** Remove all icons from the radar vertices; use text labels only.
### **II. UI LAYOUT & SPATIAL HIERARCHY**
 * **Footer Realignment:** * Move the @worldstat.visualised handle to the line directly below the "Read caption" call-to-action.
   * Translate the entire footer block (Heading, Subheading, Caption) **downwards** by 50px to accommodate larger titles.
 * **Pie Chart Scaling:** Increase the default radius R_{pie} by 30\%. Minimize dead space to ensure the chart dominates the viewport.
 * **Graph Boundaries:** Render a 1px sharp border (opacity 15\%) around the rectangular container of the graph/bar area.
### **III. MOTION & RENDERING LOGIC**
 * **Global Easing:** Apply **Ease-In-Out** (Cubic) acceleration and deceleration to ALL chart types (Pie, Radar, Line, Bar).
 * **Aesthetic Reset:** Remove all "Side Glow/Bloom" from the **Bar Chart** module. All bars must have sharp, clean edges.
 * **Final Frame Hold:** Append a **3-second static freeze** at the end of the video duration (t_{max}) so users can analyze the final data state.
 * **Text Collision (Line Graph):** Fix the overlap logic. Implement a recursive check: if bbox_a intersects bbox_b, apply a vertical offset of \pm 12px until zero intersection is achieved.
### **IV. SYSTEM FEATURES & BATCH PROCESSING**
 * **Multi-Type Preview:** Integrate the render_preview function for Radar and Pie charts. The UI must display a static frame of the final data state for all chart types before the user commits to rendering.
 * **Dynamic Batch Interface:** * Implement a dynamic UI list where users can "Add" or "Remove" input blocks.
   * Each block must contain unique fields for: CSV_URL, TITLE, and a CHART_TYPE selector (Bar, Line, Pie, Radar).
 * **Error Reporting:** Wrap the rendering engine in a global try-except block. On failure, trigger a modal displaying the **Exception Type** and the **Traceback** to the user.
### **Regarding your Hosting Needs:**
Since you mentioned the old "keep awake" systems, the most reliable **free** way to do this in 2026 without the "UptimeRobot" loophole is **Hugging Face Spaces**.
 1. **Hugging Face Spaces** is essentially a free hosting site where you can host your Replit code.
 2. It uses **Docker**, meaning it won't "sleep" as long as people are using it, and it gives you more RAM than Replit's free tier for video rendering.
 3. You just link your GitHub repo to it, and it goes live. No "keep awake" scripts required.
Would you like the specific Dockerfile settings to make that hosting work once the agent finishes these fixes?
